"use client";
import { useState } from "react";
import { Send } from "lucide-react";
export default function ContactForm({captchaEnabled}:{captchaEnabled:boolean}){const [a]=useState(()=>Math.floor(Math.random()*5)+2);const [b]=useState(()=>Math.floor(Math.random()*5)+1);const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus("");const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),phone:f.get("phone"),message:f.get("message"),website:f.get("website"),captchaEnabled,a,b,answer:Number(f.get("answer"))})});const x=await r.json();setStatus(r.ok?"Gracias. Tu mensaje fue enviado.":x.error);if(r.ok)e.currentTarget.reset();setBusy(false)}return <form className="contact-form" onSubmit={submit}><div className="contact-form-grid"><label>Nombre<input name="name" required/></label><label>Correo electrónico<input name="email" type="email" required/></label><label>Teléfono<input name="phone" type="tel"/></label><label className="honeypot">Sitio web<input name="website" tabIndex={-1} autoComplete="off"/></label></div><label>Mensaje<textarea name="message" required minLength={5}/></label>{captchaEnabled&&<label className="captcha-row"><span>Verificación: ¿cuánto es {a} + {b}?</span><input name="answer" type="number" required inputMode="numeric"/></label>}<div className="contact-submit"><button disabled={busy}><Send/>{busy?"Enviando…":"Enviar consulta"}</button>{status&&<p role="status">{status}</p>}</div></form>}
